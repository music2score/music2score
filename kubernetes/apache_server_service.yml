apiVersion: v1
kind: Service
metadata:
  name: apache-server
  namespace: music2score
spec:
  type: NodePort
  ports:
  - name: default
    protocol: TCP
    port: 80
    targetPort: 80
  selector:
    tier: webapp